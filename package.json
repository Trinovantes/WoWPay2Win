{
  "scripts": {
    "clean": "find dist cache -maxdepth 1 -type f -not -path '*/.*' | xargs rm",
    "build": "parcel build index.html --no-source-maps",
    "dev": "parcel index.html"
  },
  "devDependencies": {
    "less": "^3.11.1",
    "parcel-plugin-handlebars": "^0.4.4"
  },
  "dependencies": {
    "jquery-tablesort": "0.0.11",
    "moment": "^2.24.0"
  }
}
